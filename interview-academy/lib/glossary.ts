import type { GlossaryTerm } from "./types";

export const glossary: GlossaryTerm[] = [
  { term: "Accuracy", def: "The fraction of predictions a classifier gets right. Misleading on imbalanced data, where a trivial majority-class model can score high while catching none of the rare class.", related: ["Precision", "Recall", "F1 score"] },
  { term: "Activation function", def: "A non-linear function (ReLU, sigmoid, tanh, GELU) applied to a neuron's weighted sum, letting neural networks model non-linear relationships. Without it a deep net collapses to a linear model.", related: ["Neural network", "ReLU", "Vanishing gradient"] },
  { term: "Adam", def: "A popular adaptive optimizer that combines momentum with per-parameter learning-rate scaling, giving fast, robust convergence — the common default for deep learning.", related: ["Gradient descent", "Optimizer", "Learning rate"] },
  { term: "Attention", def: "A mechanism where each token computes a weighted combination of other tokens' values, with weights from query·key similarity. Self-attention is the core of the Transformer.", related: ["Transformer", "Self-attention", "Query/Key/Value"] },
  { term: "AUC (ROC-AUC)", def: "Area under the ROC curve — a threshold-independent score of how well a classifier ranks positives above negatives. 1.0 is perfect, 0.5 is random.", related: ["ROC curve", "Precision", "Recall"] },
  { term: "Backpropagation", def: "The algorithm that computes gradients of the loss with respect to every weight by applying the chain rule backward through the network, enabling gradient-descent training.", related: ["Gradient descent", "Neural network"] },
  { term: "Batch normalization", def: "Normalizing layer inputs across a mini-batch to stabilize and speed up training, reducing sensitivity to initialization and allowing higher learning rates.", related: ["Neural network", "Regularization"] },
  { term: "Bias (statistical)", def: "Error from overly simple model assumptions; high bias causes underfitting — poor performance on both training and test data.", related: ["Variance (statistical)", "Bias–variance tradeoff", "Underfitting"] },
  { term: "Bias–variance tradeoff", def: "The tension between a model too simple to capture the pattern (bias) and too sensitive to training noise (variance). Total error ≈ bias² + variance + irreducible noise.", related: ["Overfitting", "Regularization", "Generalization"] },
  { term: "BERT", def: "A bidirectional Transformer encoder pretrained with masked-language modeling. Great for understanding tasks (classification, NER) where the whole input is available at once.", related: ["Transformer", "GPT", "Self-supervised learning"] },
  { term: "Boosting", def: "An ensemble method that trains models sequentially, each correcting the previous one's errors (e.g. gradient boosting, XGBoost). Often top performer on tabular data.", related: ["Ensemble", "Random forest", "Decision tree"] },
  { term: "Chain-of-thought", def: "Prompting an LLM to reason step by step before answering, improving performance on multi-step problems by giving the model 'space to think'.", related: ["Prompt engineering", "LLM", "Few-shot learning"] },
  { term: "Confusion matrix", def: "A 2×2 table of true/false positives and negatives from which precision, recall, F1, and other classification metrics are derived.", related: ["Precision", "Recall", "F1 score"] },
  { term: "Cross-entropy loss", def: "The standard loss for classification; heavily penalizes confident wrong predictions by scoring predicted probabilities against true labels.", related: ["Loss function", "Softmax", "Supervised learning"] },
  { term: "Cross-validation", def: "Rotating which fold of the data serves as validation k times and averaging, giving a stable performance estimate that uses all data — valuable when data is limited.", related: ["Data leakage", "Overfitting", "Hyperparameter"] },
  { term: "CNN", def: "Convolutional Neural Network — uses learned filters that slide over the input to detect local patterns (edges → shapes → objects). The workhorse of computer vision.", related: ["Neural network", "Activation function"] },
  { term: "Data leakage", def: "When information from the test set or the future contaminates training (e.g. scaling on the full dataset, or a feature encoding the label), inflating scores that collapse in production.", related: ["Cross-validation", "Overfitting"] },
  { term: "Decision tree", def: "A model that splits data by feature thresholds into a tree of decisions. Interpretable but prone to overfitting alone; powerful in ensembles.", related: ["Random forest", "Boosting", "Overfitting"] },
  { term: "Dropout", def: "A regularization technique that randomly disables neurons during training so the network can't over-rely on any single path, improving generalization.", related: ["Regularization", "Overfitting", "Neural network"] },
  { term: "Embedding", def: "A dense vector representation of a token, word, image, or item where semantic similarity becomes geometric closeness. The bridge between raw data and models.", related: ["Vector database", "Attention", "RAG"] },
  { term: "Ensemble", def: "Combining multiple models to beat any single one — via bagging (parallel, e.g. random forest) or boosting (sequential, e.g. XGBoost).", related: ["Random forest", "Boosting", "Bias–variance tradeoff"] },
  { term: "Epoch", def: "One full pass of the training algorithm over the entire training dataset. Training usually runs for many epochs.", related: ["Gradient descent", "Learning rate"] },
  { term: "F1 score", def: "The harmonic mean of precision and recall, giving one balanced number — useful when classes are imbalanced and you care about both false positives and false negatives.", related: ["Precision", "Recall", "Confusion matrix"] },
  { term: "Feature engineering", def: "Creating, transforming, and selecting input variables to make patterns easier for a model to learn — often the highest-leverage work on tabular problems.", related: ["Data leakage", "Regularization", "PCA"] },
  { term: "Few-shot learning", def: "Giving an LLM a handful of examples in the prompt to demonstrate the task, steering its behavior without any weight updates.", related: ["Prompt engineering", "Chain-of-thought", "LLM"] },
  { term: "Fine-tuning", def: "Continuing training of a pretrained model on a smaller, task-specific dataset to specialize it — cheaper and less data-hungry than training from scratch.", related: ["Transfer learning", "LoRA", "RLHF"] },
  { term: "Generalization", def: "How well a model performs on data it never saw during training — the true goal of learning, distinct from fitting the training set.", related: ["Overfitting", "Bias–variance tradeoff", "Regularization"] },
  { term: "GPT", def: "Generative Pretrained Transformer — a decoder-only Transformer trained to predict the next token. The architecture behind modern generative LLMs.", related: ["Transformer", "BERT", "LLM"] },
  { term: "Gradient descent", def: "The optimization algorithm that iteratively updates weights opposite the loss gradient (θ := θ − η·∇J) to minimize error. The engine under most ML training.", related: ["Loss function", "Learning rate", "Backpropagation"] },
  { term: "Hallucination", def: "When an LLM produces fluent but false or fabricated content. Mitigated with retrieval grounding (RAG), tool use, and careful prompting/evaluation.", related: ["RAG", "LLM"] },
  { term: "Hyperparameter", def: "A setting chosen before training (learning rate, tree depth, regularization strength, batch size) rather than learned — tuned on validation data.", related: ["Learning rate", "Cross-validation", "Regularization"] },
  { term: "Imbalanced data", def: "A dataset where one class vastly outnumbers others (e.g. fraud). Requires metrics beyond accuracy (precision/recall, PR-AUC) and techniques like resampling or class weights.", related: ["Accuracy", "Precision", "Recall"] },
  { term: "Learning rate", def: "The step size η in gradient descent — the most important hyperparameter. Too small stalls training; too large diverges.", related: ["Gradient descent", "Hyperparameter", "Adam"] },
  { term: "LLM", def: "Large Language Model — a Transformer with billions of parameters pretrained on massive text to predict the next token, then aligned to follow instructions.", related: ["Transformer", "GPT", "RLHF"] },
  { term: "Loss function", def: "A function scoring how wrong a model's predictions are (MSE for regression, cross-entropy for classification). Training minimizes it.", related: ["Gradient descent", "Cross-entropy loss", "MSE / RMSE"] },
  { term: "LoRA", def: "Low-Rank Adaptation — a parameter-efficient fine-tuning method that trains small added matrices while freezing the base model, cutting cost and memory dramatically.", related: ["Fine-tuning", "LLM"] },
  { term: "MLOps", def: "The practices and tooling for reliably deploying, monitoring, and maintaining ML in production — versioning, CI/CD/CT, experiment tracking, and drift detection.", related: ["Model drift", "Model registry"] },
  { term: "Model drift", def: "Degradation of a deployed model as the world changes — data drift (inputs shift) or concept drift (the input→output relationship shifts). Triggers retraining.", related: ["MLOps", "Model registry"] },
  { term: "Model registry", def: "A versioned store of trained models with their metrics, lineage, and stage (staging/production) — the source of truth for what's deployed.", related: ["MLOps", "Model drift"] },
  { term: "MSE / RMSE", def: "Mean squared error (and its root) — regression losses/metrics that heavily penalize large errors. RMSE is in the target's original units.", related: ["Loss function", "MAE", "R²"] },
  { term: "MAE", def: "Mean absolute error — a regression metric averaging absolute differences; more robust to outliers than MSE and easier to interpret.", related: ["MSE / RMSE", "R²"] },
  { term: "Neural network", def: "A model of layered interconnected 'neurons', each computing a weighted sum plus a non-linear activation, trained end-to-end via backpropagation.", related: ["Backpropagation", "Activation function", "CNN"] },
  { term: "Overfitting", def: "When a model memorizes training data (including noise) and fails on new data — high variance. Cured with more data, regularization, or a simpler model.", related: ["Bias–variance tradeoff", "Regularization", "Generalization"] },
  { term: "PCA", def: "Principal Component Analysis — an unsupervised technique that projects data onto the directions of greatest variance to reduce dimensionality while preserving information.", related: ["Unsupervised learning", "Feature engineering"] },
  { term: "Precision", def: "Of the items a model predicts positive, the fraction that truly are: TP/(TP+FP). Favor it when false positives are costly (e.g. spam filtering).", related: ["Recall", "F1 score", "Confusion matrix"] },
  { term: "Prompt engineering", def: "Designing the input to an LLM — instructions, examples, structure, and context — to reliably get the desired output without changing the model's weights.", related: ["Few-shot learning", "Chain-of-thought", "RAG"] },
  { term: "R²", def: "Coefficient of determination — the fraction of target variance a regression model explains. 1.0 is perfect, 0 is no better than predicting the mean, and it can go negative.", related: ["MSE / RMSE", "MAE"] },
  { term: "RAG", def: "Retrieval-Augmented Generation — retrieve relevant documents and feed them into the LLM's prompt so answers are grounded in real data, fighting hallucination without retraining.", related: ["Embedding", "Vector database", "Hallucination"] },
  { term: "Random forest", def: "An ensemble of decision trees trained on random data/feature subsets (bagging), averaged to reduce variance. Strong, low-tuning baseline for tabular data.", related: ["Decision tree", "Ensemble", "Boosting"] },
  { term: "Recall", def: "Of all actual positives, the fraction the model catches: TP/(TP+FN). Favor it when missing a positive is costly (e.g. disease screening).", related: ["Precision", "F1 score", "Confusion matrix"] },
  { term: "Regularization", def: "Techniques (L1/L2 penalties, dropout, early stopping) that constrain a model to reduce overfitting and improve generalization.", related: ["Overfitting", "Generalization", "Dropout"] },
  { term: "Reinforcement learning", def: "Learning by an agent taking actions in an environment to maximize a cumulative reward, with no fixed labeled dataset. Powers game-play, robotics, and RLHF.", related: ["RLHF", "Supervised learning"] },
  { term: "ReLU", def: "Rectified Linear Unit, f(x)=max(0,x) — the default activation for deep nets: cheap, and it largely avoids the vanishing-gradient problem of sigmoid/tanh.", related: ["Activation function", "Vanishing gradient", "Neural network"] },
  { term: "RLHF", def: "Reinforcement Learning from Human Feedback — training a reward model from human preference comparisons, then optimizing an LLM against it to make it helpful and aligned.", related: ["Reinforcement learning", "LLM", "Fine-tuning"] },
  { term: "ROC curve", def: "A plot of true-positive rate vs false-positive rate as the decision threshold varies; its area (AUC) summarizes classifier ranking quality.", related: ["AUC (ROC-AUC)", "Precision", "Recall"] },
  { term: "Self-supervised learning", def: "Learning where the data creates its own labels (predict the next token, reconstruct a masked input). The pretraining paradigm behind modern LLMs and vision models.", related: ["LLM", "Supervised learning", "BERT"] },
  { term: "Softmax", def: "A function turning a vector of scores into a probability distribution that sums to 1 — the standard final layer for multi-class classification and attention weights.", related: ["Cross-entropy loss", "Attention"] },
  { term: "Supervised learning", def: "Learning a mapping from inputs to known labels (X→y). Splits into classification (discrete target) and regression (continuous target).", related: ["Unsupervised learning", "Reinforcement learning", "Cross-entropy loss"] },
  { term: "Tokenization", def: "Splitting text into tokens (sub-word units) that an LLM processes. Token count drives context limits and API cost.", related: ["LLM", "Embedding"] },
  { term: "Transformer", def: "The neural architecture built on self-attention that processes sequences in parallel, enabling large-scale pretraining. Basis of BERT, GPT, and modern LLMs.", related: ["Attention", "GPT", "BERT"] },
  { term: "Transfer learning", def: "Reusing a model pretrained on a large general dataset as the starting point for a new task, via fine-tuning or feature extraction — data- and compute-efficient.", related: ["Fine-tuning", "Embedding"] },
  { term: "Underfitting", def: "When a model is too simple to capture the underlying pattern — high bias, poor on both training and test data. Fix with more capacity or features.", related: ["Bias (statistical)", "Overfitting", "Bias–variance tradeoff"] },
  { term: "Unsupervised learning", def: "Finding structure in unlabeled data — clustering (grouping) or dimensionality reduction (compressing). No target variable.", related: ["PCA", "Supervised learning", "Feature engineering"] },
  { term: "Vanishing gradient", def: "When gradients shrink toward zero through many layers, stalling learning in early layers. Mitigated by ReLU, residual connections, and normalization.", related: ["Backpropagation", "ReLU", "Activation function"] },
  { term: "Variance (statistical)", def: "Error from a model's over-sensitivity to the particular training data; high variance causes overfitting.", related: ["Bias (statistical)", "Overfitting", "Regularization"] },
  { term: "Vector database", def: "A store optimized for similarity search over embeddings (e.g. FAISS, Pinecone), enabling fast nearest-neighbor retrieval — the backbone of RAG.", related: ["Embedding", "RAG"] },
];
