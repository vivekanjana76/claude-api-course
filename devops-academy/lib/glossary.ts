import type { GlossaryTerm } from "./types";

export const glossary: GlossaryTerm[] = [
  { term: "Argo CD", def: "A declarative GitOps continuous-delivery tool for Kubernetes that continuously syncs cluster state to manifests stored in a Git repository, auto-detecting and correcting drift.", related: ["GitOps", "Kubernetes", "Drift"] },
  { term: "Artifact", def: "A file produced by a pipeline stage — a container image, a compiled binary, a package, or a test report — that is versioned and passed to later stages.", related: ["CI/CD", "Registry"] },
  { term: "Blue-green deployment", def: "A release strategy that stands up a new version (green) alongside the old (blue) and switches all traffic at once, enabling instant rollback at the cost of doubled capacity.", related: ["Canary deployment", "Rolling update"] },
  { term: "Branch protection", def: "GitHub rules on a branch (e.g. main) that require passing CI checks and code review before a merge, making the main branch always releasable.", related: ["Pull request", "Quality gate"] },
  { term: "CALMS", def: "A framework for assessing DevOps maturity: Culture, Automation, Lean, Measurement, Sharing.", related: ["DevOps", "Three Ways"] },
  { term: "Canary deployment", def: "Releasing a new version to a small percentage of traffic first, watching metrics, then ramping up — limiting the blast radius of a bad deploy.", related: ["Blue-green deployment", "Progressive delivery"] },
  { term: "cgroups", def: "A Linux kernel feature that limits and meters the resources (CPU, memory, I/O) a group of processes can use — one of the primitives underpinning containers.", related: ["Container", "Namespaces"] },
  { term: "CI/CD", def: "Continuous Integration (build + test every change automatically) and Continuous Delivery/Deployment (automatically release passing changes). The automated path from commit to production.", related: ["Continuous integration", "Pipeline"] },
  { term: "ConfigMap", def: "A Kubernetes object holding non-sensitive configuration (URLs, flags) injected into pods as environment variables or files, keeping config out of the image.", related: ["Secret", "Kubernetes"] },
  { term: "Container", def: "A lightweight, isolated package of an app plus its dependencies that shares the host OS kernel, starting in milliseconds and running identically anywhere.", related: ["Docker", "Image", "Kubernetes"] },
  { term: "Container runtime", def: "The software that pulls images and runs containers (containerd, CRI-O). Kubernetes talks to it via the CRI interface.", related: ["Container", "Kubernetes"] },
  { term: "Continuous delivery", def: "Every change that passes the pipeline is deployable and auto-deployed to staging, with a human approving the release to production.", related: ["Continuous deployment", "CI/CD"] },
  { term: "Continuous deployment", def: "Every change that passes the pipeline auto-deploys all the way to production with no manual gate. Requires strong automated testing.", related: ["Continuous delivery", "CI/CD"] },
  { term: "Continuous integration", def: "Automatically building and testing every change as it's merged to the main branch frequently, catching integration problems early.", related: ["CI/CD", "Quality gate"] },
  { term: "Control plane", def: "The 'brain' of a Kubernetes cluster — API server, etcd, scheduler, and controller manager — that makes scheduling and reconciliation decisions.", related: ["Kubernetes", "etcd", "Node"] },
  { term: "DaemonSet", def: "A Kubernetes workload that runs exactly one pod on every node — used for node-level agents like log collectors and monitoring.", related: ["Deployment", "Kubernetes"] },
  { term: "Declarative", def: "Describing the desired end state (what) rather than the steps to reach it (how). Kubernetes, Terraform, and GitOps are all declarative.", related: ["Reconciliation", "Idempotent"] },
  { term: "Deployment (K8s)", def: "The Kubernetes object for stateless apps: it manages ReplicaSets to keep N replicas running and performs zero-downtime rolling updates and rollbacks.", related: ["ReplicaSet", "Pod", "Rolling update"] },
  { term: "DevOps", def: "A culture and set of practices uniting development and operations to build, ship, and run software continuously — through shared ownership, automation, and fast feedback.", related: ["CALMS", "SRE", "CI/CD"] },
  { term: "Dockerfile", def: "A text recipe of instructions (FROM, RUN, COPY, CMD…) that Docker executes to build an image, each instruction adding a layer.", related: ["Image", "Layer", "Multi-stage build"] },
  { term: "DORA metrics", def: "The four key measures of software delivery performance: deployment frequency, lead time for changes, change failure rate, and mean time to restore (MTTR).", related: ["DevOps", "MTTR"] },
  { term: "Drift", def: "When the real state of infrastructure or a cluster diverges from the state declared in code. GitOps and Terraform detect and correct drift.", related: ["GitOps", "Terraform", "Reconciliation"] },
  { term: "etcd", def: "The consistent, distributed key-value store that holds all Kubernetes cluster state — the single source of truth, written to only by the API server.", related: ["Control plane", "Kubernetes"] },
  { term: "Feature flag", def: "A runtime toggle that decouples deploy from release — code ships dark and is enabled for a cohort without a redeploy, providing an instant kill switch.", related: ["Canary deployment", "Progressive delivery"] },
  { term: "GitHub Actions", def: "GitHub's built-in CI/CD: YAML workflows in .github/workflows/ triggered by repository events, running jobs of steps on runners.", related: ["CI/CD", "Runner", "Workflow"] },
  { term: "GitOps", def: "An operating model where Git is the single source of truth for infrastructure and apps; an agent continuously reconciles the live system to the repo.", related: ["Argo CD", "Declarative", "Drift"] },
  { term: "Horizontal Pod Autoscaler (HPA)", def: "A Kubernetes controller that automatically adjusts a Deployment's replica count based on CPU, memory, or custom metrics between a min and max.", related: ["Deployment", "Autoscaling"] },
  { term: "Idempotent", def: "An operation that produces the same result whether run once or many times — a core property of declarative infrastructure tools like Terraform and Ansible.", related: ["Declarative", "Terraform"] },
  { term: "Image", def: "A read-only template of stacked, content-addressed layers that a container is created from. The versioned artifact you build, push, and deploy.", related: ["Container", "Layer", "Registry"] },
  { term: "Infrastructure as Code (IaC)", def: "Defining and provisioning infrastructure through version-controlled, declarative code instead of manual clicks — reproducible, reviewable, and auditable.", related: ["Terraform", "Declarative", "Drift"] },
  { term: "Ingress", def: "A Kubernetes object that routes external HTTP(S) traffic to Services by hostname and path and terminates TLS, implemented by an ingress controller (NGINX, Traefik).", related: ["Service", "Kubernetes"] },
  { term: "Job / CronJob", def: "Kubernetes objects for run-to-completion tasks (Job) and scheduled tasks (CronJob) — batch work rather than long-running services.", related: ["Deployment", "Kubernetes"] },
  { term: "kubectl", def: "The command-line client for Kubernetes that talks to the API server: apply, get, describe, logs, rollout, and more.", related: ["Kubernetes", "Control plane"] },
  { term: "kubelet", def: "The agent on each worker node that talks to the API server and ensures the pods assigned to the node are running and healthy.", related: ["Node", "Kubernetes"] },
  { term: "Kubernetes", def: "An open-source container orchestrator that declaratively schedules, scales, heals, and rolls out containers across a cluster of machines.", related: ["Container", "Control plane", "Pod"] },
  { term: "Layer", def: "A read-only filesystem diff created by one Dockerfile instruction. Layers are content-addressed, cached, and shared across images.", related: ["Image", "Dockerfile"] },
  { term: "Lead time for changes", def: "A DORA metric: the time from a commit to that change running in production. Elite teams achieve under an hour.", related: ["DORA metrics", "Deployment frequency"] },
  { term: "Liveness probe", def: "A Kubernetes health check that determines whether a container is alive; on failure Kubernetes restarts the container.", related: ["Readiness probe", "Kubernetes"] },
  { term: "Module (Terraform)", def: "A reusable, parameterized package of Terraform configuration — the unit of composition and reuse for infrastructure code.", related: ["Terraform", "Infrastructure as Code"] },
  { term: "MTTR", def: "Mean Time To Restore — a DORA stability metric measuring how quickly a team recovers from a production incident. Elite teams: under an hour.", related: ["DORA metrics", "Incident response"] },
  { term: "Multi-stage build", def: "A Dockerfile technique that builds with a full toolchain in one stage and copies only the finished artifact into a tiny runtime stage — smaller, more secure images.", related: ["Dockerfile", "Image"] },
  { term: "Namespaces (Linux)", def: "A Linux kernel feature that isolates what a process can see (PIDs, network, mounts) — a primitive that makes container isolation possible.", related: ["cgroups", "Container"] },
  { term: "Node", def: "A worker machine (VM or bare metal) in a Kubernetes cluster that runs pods, via kubelet, a container runtime, and kube-proxy.", related: ["Kubernetes", "Pod", "kubelet"] },
  { term: "Observability", def: "The ability to understand a system's internal state from its outputs — metrics, logs, and traces (the three pillars). Broader than monitoring.", related: ["Metrics", "Tracing", "SLO"] },
  { term: "OCI", def: "The Open Container Initiative — the open standards for container image format and runtime that make images portable across Docker, containerd, and others.", related: ["Container", "Image"] },
  { term: "Pipeline", def: "The automated, code-defined process that turns a commit into a tested, deployed service — a sequence of jobs and stages with quality gates.", related: ["CI/CD", "Quality gate", "Runner"] },
  { term: "Pod", def: "The smallest deployable unit in Kubernetes: one or more containers sharing a network identity and storage, always scheduled together. Ephemeral and disposable.", related: ["Deployment", "Container", "Node"] },
  { term: "Progressive delivery", def: "Advanced deployment (automated canary, blue-green) driven by metrics so releases roll forward or back automatically — often run by Argo Rollouts or Flagger.", related: ["Canary deployment", "Feature flag"] },
  { term: "Pull request (PR)", def: "A proposal to merge a Git branch where code review, automated CI checks, and discussion happen before code reaches the main branch.", related: ["Branch protection", "Continuous integration"] },
  { term: "Quality gate", def: "An automated check (lint, test, coverage, security scan) that must pass before a change advances through the pipeline.", related: ["Pipeline", "Branch protection", "Shift left"] },
  { term: "Readiness probe", def: "A Kubernetes health check that determines whether a pod is ready for traffic; on failure the pod is removed from Service endpoints without being restarted.", related: ["Liveness probe", "Service"] },
  { term: "Reconciliation", def: "The continuous loop of observing actual state, comparing to desired state, and acting to close the gap — the engine behind Kubernetes and GitOps.", related: ["Declarative", "GitOps", "Drift"] },
  { term: "Registry", def: "A versioned store for container images (Docker Hub, GHCR, ECR/ACR). CI pushes images to it; hosts and clusters pull from it to deploy.", related: ["Image", "Artifact"] },
  { term: "ReplicaSet", def: "A Kubernetes controller that keeps a specified number of identical pod replicas running; usually managed by a Deployment rather than created directly.", related: ["Deployment", "Pod"] },
  { term: "Rolling update", def: "A deployment strategy (Kubernetes' default) that incrementally replaces old pods with new ones for zero-downtime releases at no extra capacity cost.", related: ["Blue-green deployment", "Canary deployment"] },
  { term: "Runner", def: "The machine that executes a CI/CD job's steps — cloud-hosted (e.g. ubuntu-latest) or self-hosted.", related: ["GitHub Actions", "Pipeline"] },
  { term: "Secret (K8s)", def: "A Kubernetes object for sensitive config (passwords, keys). Only base64-encoded by default — enable encryption at rest, RBAC, or an external manager.", related: ["ConfigMap", "Kubernetes"] },
  { term: "Service (K8s)", def: "A Kubernetes object giving a set of label-selected pods a stable virtual IP and DNS name, load-balancing across the healthy ones. Types: ClusterIP, NodePort, LoadBalancer.", related: ["Ingress", "Pod", "Kubernetes"] },
  { term: "Shift left", def: "Moving checks — tests, security scans — as early in the pipeline as possible so defects are caught when they are cheapest to fix.", related: ["Quality gate", "CI/CD"] },
  { term: "SLI / SLO", def: "A Service Level Indicator is a measured signal (e.g. request latency); a Service Level Objective is a target for it (e.g. 99.9% under 300ms). SLOs drive error budgets.", related: ["Error budget", "Observability", "SRE"] },
  { term: "Error budget", def: "The allowed amount of unreliability (1 − SLO). While budget remains, ship features; when it's exhausted, prioritize reliability. Balances speed and stability.", related: ["SLI / SLO", "SRE"] },
  { term: "SRE", def: "Site Reliability Engineering — Google's implementation of DevOps that applies software engineering (SLOs, error budgets, toil reduction) to operations.", related: ["DevOps", "SLI / SLO", "Toil"] },
  { term: "State (Terraform)", def: "The file mapping Terraform's declared resources to real-world infrastructure, letting it compute a plan. Stored remotely and locked for team use.", related: ["Terraform", "Infrastructure as Code"] },
  { term: "StatefulSet", def: "A Kubernetes workload for stateful apps (databases) giving each pod a stable network identity and persistent per-pod storage.", related: ["Deployment", "Kubernetes"] },
  { term: "Terraform", def: "A declarative Infrastructure-as-Code tool using HCL to provision cloud resources; it computes and applies the diff between declared config and real state.", related: ["Infrastructure as Code", "State (Terraform)", "Provider"] },
  { term: "Provider (Terraform)", def: "A plugin that lets Terraform manage a specific platform's resources (AWS, Azure, GCP, Kubernetes) via its API.", related: ["Terraform", "Infrastructure as Code"] },
  { term: "Three Ways", def: "The principles behind DevOps: Flow (Dev→Ops→customer), Feedback (fast signals right-to-left), and Continual learning and experimentation.", related: ["DevOps", "CALMS"] },
  { term: "Toil", def: "Manual, repetitive, automatable operational work with no lasting value. SRE seeks to reduce toil through automation so engineers do engineering.", related: ["SRE", "Automation"] },
  { term: "Tracing", def: "Following a single request across services to see where time is spent — the third pillar of observability, alongside metrics and logs.", related: ["Observability", "Metrics"] },
  { term: "Trunk-based development", def: "A branching strategy where everyone integrates small changes to main frequently via short-lived branches — best suited to continuous delivery.", related: ["Pull request", "Continuous integration"] },
  { term: "DevSecOps", def: "Extending DevOps to make security a continuous, automated, shared responsibility built into every pipeline stage — rather than a manual gate before release.", related: ["Shift left", "SAST / DAST / SCA", "Policy as code"] },
  { term: "SAST / DAST / SCA", def: "The core automated security scans: SAST reads source code statically, DAST probes the running app from outside, and SCA analyzes third-party dependencies for known CVEs.", related: ["DevSecOps", "Shift left", "CVE"] },
  { term: "CVE", def: "A Common Vulnerabilities and Exposures identifier — a public, uniquely numbered entry for a known security flaw, used by scanners to flag vulnerable packages.", related: ["SAST / DAST / SCA", "Image scanning", "SBOM"] },
  { term: "Image scanning", def: "Inspecting a container image's OS packages and app dependencies against vulnerability databases (e.g. Trivy, Grype) — run in CI and gated on fixable high/critical CVEs.", related: ["CVE", "Supply chain security", "SBOM"] },
  { term: "SBOM", def: "A Software Bill of Materials — a machine-readable inventory of every component in an artifact, letting you instantly determine whether you're affected by a newly disclosed CVE.", related: ["Supply chain security", "Image scanning", "CVE"] },
  { term: "Supply chain security", def: "Establishing trust across everything around your code — base images, dependencies, and the build process — since modern attacks often target the chain rather than the app.", related: ["SBOM", "Image signing", "SLSA"] },
  { term: "Image signing", def: "Cryptographically signing an artifact (e.g. with cosign/Sigstore) so the cluster can verify at deploy time that it's the genuine, untampered image the pipeline produced.", related: ["Supply chain security", "SLSA", "Policy as code"] },
  { term: "SLSA", def: "Supply-chain Levels for Software Artifacts — a framework of increasing guarantees whose higher levels require tamper-proof, automatically generated build provenance.", related: ["Supply chain security", "Image signing", "SBOM"] },
  { term: "Secret manager", def: "A system (HashiCorp Vault, cloud Secrets Manager) that stores secrets encrypted, gates access with policy, audits every access, and can issue short-lived dynamic secrets.", related: ["Secret (K8s)", "Sealed Secrets", "Secrets management"] },
  { term: "Sealed Secrets", def: "A tool that encrypts a Kubernetes Secret so only the in-cluster controller can decrypt it, making the encrypted blob safe to commit to Git — fitting the GitOps model.", related: ["Secret manager", "GitOps", "Secrets management"] },
  { term: "Secrets management", def: "Keeping credentials out of source code and delivering them safely at runtime via managers, external/sealed secrets, rotation, and least privilege.", related: ["Secret manager", "Sealed Secrets", "Secret (K8s)"] },
  { term: "RBAC (K8s)", def: "Role-Based Access Control — Roles/ClusterRoles define allowed verbs on resources, and RoleBindings grant them to subjects (users or workload ServiceAccounts). The basis of least privilege.", related: ["ServiceAccount", "Least privilege", "Kubernetes"] },
  { term: "ServiceAccount", def: "The identity a pod runs as when calling the Kubernetes API. Give each workload its own with only the permissions it needs — never bind cluster-admin.", related: ["RBAC (K8s)", "Least privilege", "Pod"] },
  { term: "NetworkPolicy", def: "A Kubernetes object that restricts pod-to-pod traffic by label selector. Since pods can reach everything by default, a default-deny policy plus explicit allows enables zero-trust networking.", related: ["Zero trust", "Kubernetes", "RBAC (K8s)"] },
  { term: "Policy as code", def: "Enforcing security/governance rules automatically at the Kubernetes admission stage — every object is checked and can be rejected. Tools: OPA/Gatekeeper, Kyverno, Pod Security Admission.", related: ["DevSecOps", "RBAC (K8s)", "Admission controller"] },
  { term: "Admission controller", def: "A Kubernetes component that intercepts API requests to validate or mutate objects before they're persisted — the enforcement point for policy-as-code.", related: ["Policy as code", "Kubernetes"] },
  { term: "Least privilege", def: "Granting each identity or workload only the permissions and access it genuinely needs, so a compromise has the smallest possible blast radius.", related: ["RBAC (K8s)", "ServiceAccount", "Zero trust"] },
  { term: "Zero trust", def: "A security model that trusts nothing by default and verifies every connection — in Kubernetes, realized by default-deny NetworkPolicies and per-workload identity.", related: ["NetworkPolicy", "Least privilege", "Defense in depth"] },
  { term: "Defense in depth", def: "Layering independent security controls (signed images, RBAC, network policy, policy-as-code, observability) so no single control has to be perfect — a breach must defeat several.", related: ["Zero trust", "Least privilege", "DevSecOps"] },
  { term: "Threat modeling", def: "Systematically asking 'how could this be abused?' before building — frameworks like STRIDE give teams a repeatable way to think like an attacker early.", related: ["DevSecOps", "Shift left"] },
  { term: "Twelve-Factor App", def: "A methodology of twelve principles for building portable, scalable services — e.g. config in the environment, stateless processes, treat logs as event streams, dev/prod parity.", related: ["Container", "ConfigMap", "Immutable artifacts"] },
  { term: "Cattle not pets", def: "A metaphor for treating servers as interchangeable and disposable (replace, don't repair) rather than uniquely cared-for machines — the mindset behind immutable infrastructure.", related: ["Immutable infrastructure", "Container"] },
  { term: "Immutable infrastructure", def: "Never modifying a running server — to change it, you build a new image/instance and replace the old one, eliminating configuration drift.", related: ["Cattle not pets", "Drift", "Image"] },
  { term: "Helm", def: "The de-facto package manager for Kubernetes: a Chart is a templated, versioned, parameterized bundle of manifests you install and upgrade as one release.", related: ["Kustomize", "Kubernetes"] },
  { term: "Kustomize", def: "A template-free way to customize Kubernetes manifests via overlays (base + per-environment patches), built into kubectl. An alternative/complement to Helm.", related: ["Helm", "Kubernetes", "GitOps"] },
  { term: "Service mesh", def: "An infrastructure layer (Istio, Linkerd) that adds mTLS, traffic control, retries, and observability to service-to-service calls via sidecar proxies — without changing app code.", related: ["Sidecar", "Zero trust", "Observability"] },
  { term: "Sidecar", def: "A helper container running alongside the main app container in the same pod, sharing its network — used for proxies, log shippers, and secret agents.", related: ["Service mesh", "Pod"] },
  { term: "Operator", def: "A custom controller that encodes operational knowledge for a specific app (e.g. a database), automating install, upgrades, and recovery via CRDs and the reconciliation loop.", related: ["CRD", "Reconciliation", "Kubernetes"] },
  { term: "CRD", def: "A Custom Resource Definition extends the Kubernetes API with your own resource types, letting you manage app-specific objects declaratively like built-in ones.", related: ["Operator", "Kubernetes"] },
  { term: "PodDisruptionBudget", def: "A policy limiting how many replicas of an app can be voluntarily taken down at once (e.g. during node drains), protecting availability during maintenance.", related: ["Deployment", "Kubernetes"] },
  { term: "Cluster Autoscaler", def: "A component that adds or removes worker nodes based on whether pods can be scheduled — it scales the cluster, while the HPA scales the pods.", related: ["Horizontal Pod Autoscaler (HPA)", "Node"] },
  { term: "Platform engineering", def: "Building an internal, self-service platform (an IDP) that gives developers paved-road tooling and golden paths, so they ship without deep infra expertise.", related: ["Golden path", "DevOps"] },
  { term: "Golden path", def: "A well-supported, opinionated default way to build and ship a service on your platform — the easy road that's also the secure, compliant one.", related: ["Platform engineering", "Shift left"] },
  { term: "Ephemeral environment", def: "A short-lived, on-demand environment (often per pull request) that's spun up to test a change and torn down after — enabled by IaC and containers.", related: ["Infrastructure as Code", "Pull request"] },
  { term: "Chaos engineering", def: "Deliberately injecting failures (killing pods, adding latency) into a system to verify it degrades gracefully and to build confidence in its resilience.", related: ["SRE", "Observability", "Blast radius"] },
  { term: "Blast radius", def: "The scope of damage a failure or compromise can cause. Least privilege, canaries, cells, and network policies all work to shrink it.", related: ["Least privilege", "Canary deployment", "Defense in depth"] },
  { term: "Golden signals", def: "Google SRE's four key service metrics to watch: latency, traffic, errors, and saturation — a superset of the RED method.", related: ["Observability", "SLI / SLO"] },
  { term: "Runbook", def: "A documented, step-by-step procedure for handling a known operational task or alert — ideally automated over time to reduce toil.", related: ["Toil", "Incident response"] },
  { term: "Semantic versioning", def: "A versioning scheme MAJOR.MINOR.PATCH where each part signals the kind of change (breaking / feature / fix), making dependency updates predictable.", related: ["Artifact", "Image"] },
];
